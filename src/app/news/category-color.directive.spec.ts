import { CategoryColorDirective } from './category-color.directive';

describe('CategoryColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryColorDirective();
    expect(directive).toBeTruthy();
  });
});
